# EntityFramework5_CosmosDB_Example
## A simple demo application showing one way of configuring EF Core with Cosmos DB

This sample works with the Cosmos DB Emulator.

Run the test to add data to the database
